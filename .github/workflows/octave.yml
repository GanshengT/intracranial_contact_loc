name: Octave Tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        run: |
          cd automated_pipeline/imaging_processing_scripts/automatic_localize
          octave --no-gui --quiet test_safe_ratio.m
